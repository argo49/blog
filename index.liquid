---
layout: homePage.liquid
pageTitle: Home
---
<p>A blog by Tyler Argo.</p>
{% for post in collections.posts %}
    <h2><a href="{{ post.url }}">{{ post.data.pageTitle }}</a></h2>
    <div>Published <time datetime="{{post.date}}">{{post.date | date: "%Y-%m-%d"}}</time></div>
{% endfor %}